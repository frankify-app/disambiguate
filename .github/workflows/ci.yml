name: CI

on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: astral-sh/setup-uv@v8.1.0
      - uses: j178/prek-action@v2.0.2

  # Make sure commit messages follow the conventional commits convention:
  # https://www.conventionalcommits.org
  commitlint:
    name: Lint Commit Messages
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
      contents: read
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0
      - uses: wagoid/commitlint-github-action@v6.2.1

  test:
    strategy:
      fail-fast: false
      matrix:
        python-version:
          # - "3.10"
          # - "3.11"
          # - "3.12"
          - "3.13"
          # - "3.14"
        os:
          - ubuntu-latest
          # - windows-latest
          # - macOS-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v8.1.0
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --no-python-downloads
        shell: bash
      - run: uv run pytest tests
        shell: bash

  # Runs the tool against this repository's own glossary and prose. The
  # unit suite proves the checks work on fixtures; this proves they hold
  # on the real corpus, which is the only place drift actually appears.
  # `--drift` is read-only (adr 0002), so nothing here writes back.
  dogfood:
    name: "dogfood (lint + drift)"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: astral-sh/setup-uv@v8.1.0
        with:
          python-version: "3.13"
      - run: uv sync --no-python-downloads
        shell: bash
      - run: make dogfood
        shell: bash

# release:
#   needs:
#     - test
#     - lint
#     - commitlint

#   runs-on: ubuntu-latest
#   environment: release
#   concurrency: release
#   permissions:
#     id-token: write
#     attestations: write
#     contents: write

#   steps:
#     - uses: actions/checkout@v6.0.2
#       with:
#         fetch-depth: 0
#         ref: ${{ github.sha }}

#     - name: Checkout commit for release
#       run: |
#         git checkout -B ${{ github.ref_name }} ${{ github.sha }}

#     # Do a dry run of PSR
#     - name: Test release
#       uses: python-semantic-release/python-semantic-release@v10.5.3
#       if: github.ref_name != 'main'
#       with:
#         no_operation_mode: true
#         github_token: noop

#     # On main branch: actual PSR + upload to PyPI & GitHub
#     - name: Release
#       uses: python-semantic-release/python-semantic-release@v10.5.3
#       if: github.ref_name == 'main'
#       id: release
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}

#     - name: Attest build provenance
#       uses: actions/attest@v4
#       if: steps.release.outputs.released == 'true'
#       with:
#         subject-path: "dist/*"

#     - name: Publish package distributions to PyPI
#       uses: pypa/gh-action-pypi-publish@v1.14.0
#       if: steps.release.outputs.released == 'true'

#     - name: Publish package distributions to GitHub Releases
#       uses: python-semantic-release/publish-action@v10.5.3
#       if: steps.release.outputs.released == 'true'
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         tag: ${{ steps.release.outputs.tag }}
